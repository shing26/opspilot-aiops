# 离线管道的开发/验证依赖（**运行时代码零第三方依赖**：chunkers 与验收脚本只用 stdlib）。
#
# 为什么要锁版本：
#  - CI 此前是 `pip install pytest`（不锁）——上游一次大版本变更就能把 CI 打红，
#    而红的原因与本次改动无关（不稳定的门闩 = 会被忽略的门闩）。
#  - locust 决定压测报告的口径与字段，随附的 `offline/load/reports/*.csv|html` 是**用它跑出来的**；
#    不锁版本会让"复现命令复现不出随附产物"。
# 锁定的版本与本机开发 venv 实测一致（2026-09-17）。
pytest==9.1.1
locust==2.46.5
