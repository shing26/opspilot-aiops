# 第三方公开仓库的 checkout 卫生（2026-09-13 S3 Linux 客串实测引入）：
# 本仓开发机 core.autocrlf=true——无此文件时 .sh 在 Windows 检出会被转成 CRLF，
# bash 直接崩（"bad interpreter: /usr/bin/env^M"）。shell/python 一律钉死 LF。
# 不配 *.bat（Windows 批处理本就需 CRLF，交 git 默认）。
*.sh        text eol=lf
*.py        text eol=lf
Dockerfile  text eol=lf
docker-compose.yml text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
.env.example text eol=lf
# 数据产物与语料（2026-09-18 引入，随 offline/provenance.py 一起来）：
# 语料 .md 与 chunks/golden .jsonl 是**被摘要的对象**。摘要按内容（CRLF→LF 归一）计算，
# 故检出约定本来不影响判定；但工作区与 blob 行尾不一致会让"人和 CI 算出的摘要为什么不同"
# 变成反复排查的坑——直接在检出层抹平，不留这个解释成本。
*.jsonl     text eol=lf
*.md        text eol=lf
# 报告类 JSON 同理（PROVENANCE.json 等）：不做这行，autocrlf 会在每次 git touch 时
# 把工作区刷成 CRLF 并刷警告；摘要判据本身已行尾归一，这里只是消灭排查噪音。
*.json      text eol=lf
