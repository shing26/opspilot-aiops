# 构建产物
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# 环境与凭据（绝不入库）
.env
*.local

# IDE
.idea/
.vscode/
*.iml

# Python
offline/.venv/
__pycache__/
*.pyc
.pytest_cache/

# 运行时产物（chunks.jsonl 与评测/压测报告按 ADR-0001 / DoD 需入库，不再排除）
logs/
*.log

# OS
Thumbs.db
.DS_Store

# 本地扫描状态
.mimosa/

# 生成物与凭据（均不入库）：
# - 红队畸形 token 由 JWT_SECRET 签名，可再生；合法账号走 login 换 token，不落盘
# - data/ 是 H2 用户主库（含 bcrypt 散列），凭据类，绝不入库
# - .zcode/ 是本地 agent 计划文件
scripts/redteam_tokens.txt
scripts/demo_tokens.txt
data/
.zcode/
